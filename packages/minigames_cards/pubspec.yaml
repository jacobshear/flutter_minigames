name: minigames_cards
description: >-
  Shared playing-card deck and renderer for flutter_minigames card
  games — suits, ranks, seeded shuffling, and a classic vector card face.
version: 0.1.0
repository: https://github.com/jacobshear/flutter_minigames
topics:
  - games
  - cards

environment:
  sdk: ^3.6.0
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
