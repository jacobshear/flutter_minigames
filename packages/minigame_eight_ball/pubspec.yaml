name: minigame_eight_ball
description: >-
  8-Ball pool for flutter_minigames — top-down Forge2D break/aim/pot logic on
  the minigames_flame seam, with a pure TurnGame outcome state.
version: 0.1.0
repository: https://github.com/jacobshear/flutter_minigames
topics:
  - games
  - turn-based
  - physics

environment:
  sdk: ^3.6.0
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.18.0
  minigames_core: ^0.1.0
  minigames_ui: ^0.1.0
  minigames_flame: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
