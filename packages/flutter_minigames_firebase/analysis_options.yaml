include: package:flutter_lints/flutter.yaml

# Excludes the Flutter tool (3.47+) writes itself on `pub get` when they are
# missing — kept checked in so CI's tree stays clean for `pub publish`.
analyzer:
  exclude:
    - build/**
