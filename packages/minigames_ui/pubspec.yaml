name: minigames_ui
description: >-
  Shared table chrome for flutter_minigames — the floating notice, the static
  pill, and the tones they share. Pure Flutter, no game logic.
version: 0.1.0
repository: https://github.com/jacobshear/flutter_minigames
topics:
  - games
  - ui
  - widgets

environment:
  sdk: ^3.6.0
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
